import styled from "styled-components";
import { motion } from "framer-motion";

const Start = styled.div`
  font-size: 100px;
`;

function App() {
  return <Start>Hello</Start>;
}

export default App;
